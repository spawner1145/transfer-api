{
  "name": "unlimited-transfer-api",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check src/worker.js && node --check server.js",
    "wrangler:dev": "wrangler dev",
    "wrangler:deploy": "wrangler deploy"
  },
  "optionalDependencies": {
    "wrangler": "^4.0.0"
  }
}
